# pygame 2.6.x has no wheel for Python 3.14 (macOS Framework builds from source
# and fail with missing SDL.h). pygame-ce is a drop-in `import pygame` fork
# with 3.10–3.14 wheels, including macOS universal2.
pygame-ce>=2.5.3
pillow>=10.0.0
cairosvg>=2.7.0
cairocffi>=1.6.0
cssselect2>=0.7.0
tinycss2>=1.2.0
defusedxml>=0.7.1
